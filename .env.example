# Stripe (Dashboard → Developers → API keys)
STRIPE_SECRET_KEY=sk_live_xxxxxxxx

# Stripe webhook signing secret (Dashboard → Developers → Webhooks → your endpoint)
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxx

# Vercel KV (Vercel Dashboard → Storage → KV → Connect to project)
KV_REST_API_URL=https://xxxxxxxx.kv.vercel-storage.com
KV_REST_API_TOKEN=xxxxxxxx

# Optional — defaults to 10000
DONATION_GOAL_DOLLARS=10000
